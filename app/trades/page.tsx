import PagePlaceholder from "@/components/PagePlaceholder";

export default function TradesPage() {
  return (
    <PagePlaceholder title="Trades" phase="Phase 8">
      Build trade proposals with CPU teams — they evaluate fairness by value, age,
      contract years, and cap fit, then accept, reject, or counter. Coming in
      Phase 8.
    </PagePlaceholder>
  );
}
